import myDefaultClass from "./main.kyro";
import { subtract } from "./main.kyro";
import myDefaultClass, { multiply as aliasForPublicClass } from "./main.kyro";
import native console from "console";

default static class MainClass {
    private int result = 0 + null;
    private Parser parser;

    public static operator +(int a): void {
        return a + b;
    }

    public static MainClass() {
        this.result = add(1, 2);
        print("This is the constructor!" + this.result);
    }

    public static method(): void {
        print("This is a method!");
    }
}

if(x is any of [2, 5, 3, 7]) {
    print(x+"is in array");
}

const int[] array = [];

if(x is none of array) {
    print(x+"isn't in array");
}